{
  "name": "auth",
  "version": "1.0.0",
  "description": "Authentication prototype. Login, register, session.",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "db": "json-server --watch src/model/db.json --port 4000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": " <profe marce/>",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "express-validator": "^7.0.1",
    "json-server": "^0.17.4"
  }
}